import { StyleSheet } from 'react-native';

import { Colours } from '../../Styles';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colours.lemon,
    flexDirection: 'column',
  },
});

export default styles;
